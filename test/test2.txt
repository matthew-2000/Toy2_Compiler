proc main(out a: integer, b: integer):
 c ^= somma(a, b);
 var c : integer;\
 if(true) then
    d ^= 3;
 endif;
endproc

func somma(a:integer, b:integer)-> integer:
     a ^= a + b; % DEVE DARE ERRORE NEL TYPE CHECKING - I PARAM SONO IMMUTABILI %
    return a + b;
endfunc

var d : integer;\
